# Server
PORT=5000
MONGO_URI=mongodb://localhost:27017/voting
JWT_SECRET=replace-me-with-a-strong-secret
JWT_EXPIRES_IN=7d
ADMIN_REGISTRATION_KEY=change-me

# Blockchain
BLOCKCHAIN_RPC=http://127.0.0.1:8545
VOTING_CONTRACT_ADDRESS=
BLOCKCHAIN_MOCK=true

# Dev toggles
SKIP_DB=false
LOG_LEVEL=info

# Frontend
FRONTEND_URL=http://localhost:5173
VITE_WALLETCONNECT_PROJECT_ID=
# Optional: enable a lightweight mock wallet for local development / CI.
# Set to 'true' to allow the app to simulate a connected wallet without remote
# WalletConnect calls. Useful for E2E or CI where external network calls are
# undesirable.
VITE_ENABLE_MOCK_WALLET=false
