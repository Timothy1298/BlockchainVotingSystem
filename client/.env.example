# Client environment example
# Copy this to `.env` in the client folder for local development and adjust values.

# Base URL where the API is available (backend)
VITE_API_BASE_URL=http://localhost:5000/api

# WalletConnect project id (optional). If set, WalletConnect will be enabled.
# Register at https://walletconnect.com/ to obtain a project id.
VITE_WALLETCONNECT_PROJECT_ID=

# Optional: enable a lightweight mock wallet for development/CI.
# Set to 'true' to include the mock connector in the built client and to
# inject a minimal `window.ethereum` provider for E2E tests.
VITE_ENABLE_MOCK_WALLET=false

# Frontend URL for CORS or previews
VITE_FRONTEND_URL=http://localhost:5173
